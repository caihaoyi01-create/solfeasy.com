# Shared music tools

Owner: practice agent. Files: src/components/music/_, src/lib/music-theory.ts, src/modules/music-practice/_, src/routes/api/music-practice.ts, tests/music-practice\*.ts, scripts/music-practice-copy.json. Do not edit shared translations or schema templates directly; create your own schema module and export it through templates via parent coordination if needed.

Reference inspected: MusicGPT body rgb(10,12,13), Inter, rounded dark prompt panel, pill controls, 40px headings. Adapt to a sheet-music learning studio using DESIGN.md tokens. Parent owns global CSS and shell; agent owns src/styles/music-tools.css imported by component.

Exports: NoteTrainer({clef?: 'treble'|'bass', lockedClef?: boolean, compact?: boolean}), MusicStaff({midi:number, clef:'treble'|'bass', reveal?:boolean}), PianoKeyboard({startMidi?:number,endMidi?:number,activeNotes?:number[],onPlay:(midi:number)=>void,disabled?:boolean}), ClefExplorer(). Pure theory exports noteName(midi), playNotes(midiNumbers:number[], duration?:number): Promise<void> or void, chordNotes(rootMidi,type,inversion) if useful. Notify parent exact exports early.

Trainer: Practice/Explore toggle, clef controls (locked on specific pages), readable cream SVG staff with correct ledger lines, random natural note; keyboard answers, score/streak/timer, skip/reveal, correct/wrong live feedback, sound mute. No subscriptions or ads inside tool. Put a small Pro link outside tool near recent results upon limit. Server persisted anonymous visitor cookie + quota shared across clefs/routes, authenticated premium bypass via server entitlement helper from lessons agent. 20 graded questions/day total; one grade per question; reveal counts incorrect so cannot repeat-score. No personal details in anonymous public activity. No fake community stats. Avoid auto-advancing before feedback can be read; next-note button.

Keyboard usable via click and physical labeled shortcuts while focused; full C4-C5 for treble, C2-C4 for bass; SVG notes position by diatonic steps. Explore clicks update staff and sound. Compact tutorial ClefExplorer compares same MIDI D4 on either clef. Audio only after gesture; clean up scheduled sounds/timers on unmount. Loading and failure states with retry and no disappearing layout. Add every UI string in bilingual JSON artifact {en:{flat keys},zh:{flat keys}} for parent merge. Read task plan and coordinate trial helper contract with lessons agent.
