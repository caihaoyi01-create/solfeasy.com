# Lessons and subscriptions

Owner: lessons agent. Files src/components/solfeasy/lessons.tsx, src/components/solfeasy/pricing.tsx, src/modules/music-learning/_, src/routes/api/music-learning.ts, src/config/pricing.ts, tests/music-learning_.ts, scripts/music-learning-copy.json, src/styles/music-learning.css. Parent owns routes, shell, metadata, translations merge, global schema and config. Do not overwrite others. Coordinate schema exports or custom independent schema files with parent.

Exports LessonsPageContent(), PricingPageContent(), helper server isMusicPro(userId) in module or equivalent; tell practice agent. Use server persisted 7-day no-card trial, idempotent per account and no retrials. Existing better-auth/session and payment/subscription engine. Trial grants lessons and unlimited practice server-side; expired trial returns free. Existing successful active subscriptions grant Pro. Never trust client plan state. Implement progress saved per user, private result preference for Pro if feasible. Use typed api-client + TanStack Query. All errors visible with recovery. Don't fake payment success or a purchased plan. Initial catalog $9/month or $72/year configurable in one catalog. Checkout actual existing endpoint, disabled/unavailable explanation if provider not configured; trial stays available.

Lessons H1 provided by parent; start free trial CTA within 200px of it. Beautiful dark studio course list and actual playable step-by-step lessons: middle C, right hand melody, rhythm, left hand/chords. At least one free lesson and premium continuation containing real exercise + completion validation. Import shared keyboard/theory from practice agent when ready. Lessons and pricing have no ad areas. Parent shell max width1140, tokens DESIGN.md. Own CSS file import. UI strings in scripts/music-learning-copy.json {en:{flat keys},zh:{flat keys}} for parent merge. Metadata exact supplied handled by parent. Parent will handle billing legal copy; coordinate wording.
